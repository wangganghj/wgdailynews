const button=document.querySelector('#update-button');const label=document.querySelector('#update-label');const updated=document.querySelector('#updated-at');function localize(){if(updated.dataset.time){updated.textContent=new Date(updated.dataset.time).toLocaleString('zh-CN')}}localize();async function poll(){const res=await fetch('/api/status');const data=await res.json();if(data.status==='running'){setTimeout(poll,1500);return}if(data.last_updated_at){updated.dataset.time=data.last_updated_at;localize()}location.reload()}button.addEventListener('click',async()=>{button.disabled=true;label.textContent='更新中…';try{const res=await fetch('/api/update',{method:'POST'});if(res.ok||res.status===409){setTimeout(poll,1000)}else{throw new Error('update failed')}}catch(e){button.disabled=false;label.textContent='更新失败，请重试'}});if(button.disabled)setTimeout(poll,1000);

